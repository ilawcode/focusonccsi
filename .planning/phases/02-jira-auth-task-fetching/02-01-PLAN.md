---
phase: 02-jira-auth-task-fetching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/config/jira-fields.json, src/app/api/jira/search/route.ts, .env.local]
autonomous: true
requirements: [INT-01, INT-02]
must_haves:
  truths:
    - "JQL search results are proxyable through the internal API"
    - "Fetched Jira issues include fields defined in the JSON configuration"
  artifacts:
    - path: "src/config/jira-fields.json"
      provides: "Configurable list of Jira fields to fetch"
    - path: "src/app/api/jira/search/route.ts"
      provides: "Secure proxy for Jira JQL search"
---

<objective>
Setup the technical foundation for Jira integration, including the dynamic field configuration and the backend proxy to handle JQL searches with the session-based PAT.

Purpose: Decouples the frontend from direct Jira API calls and allows for easy updates to targeted data fields.
Output: JSON configuration file and a functional search proxy API.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-jira-auth-task-fetching/02-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Define Jira Field Configuration</name>
  <files>src/config/jira-fields.json</files>
  <read_first>.planning/phases/02-jira-auth-task-fetching/02-CONTEXT.md</read_first>
  <action>Create `src/config/jira-fields.json`. This file should contain an array of objects or a simple array of strings representing the Jira field internal names (e.g., ["summary", "status", "description", "priority", "assignee", "created", "updated"]). This will be used in the `fields` parameter of Jira API calls.</action>
  <verify>test -f src/config/jira-fields.json</verify>
  <acceptance_criteria>
    - src/config/jira-fields.json exists and contains at least "summary" and "status"
  </acceptance_criteria>
  <done>JSON config created</done>
</task>

<task type="auto">
  <name>Task 2: Implement Jira Search Proxy API</name>
  <files>src/app/api/jira/search/route.ts</files>
  <read_first>src/config/jira-fields.json</read_first>
  <action>Create `src/app/api/jira/search/route.ts`. This route should accept a POST request containing a `jql` string and the `token` (PAT). It should load the fields from `src/config/jira-fields.json` and make a fetch request to `${JIRA_INSTANCE_URL}/rest/api/3/search`. It should return the list of issues found. Ensure `JIRA_INSTANCE_URL` is parsed from env.</action>
  <verify>grep "fetch" src/app/api/jira/search/route.ts</verify>
  <acceptance_criteria>
    - Route accepts JQL and token
    - Proxy uses the fields defined in the JSON config
  </acceptance_criteria>
  <done>Search proxy API implemented</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] API route returns data when provided with a valid JQL and token (can be mocked or tested via local runner).
</verification>

<success_criteria>
- All tasks completed
- Backend proxy is ready for frontend consumption
</success_criteria>

<output>
After completion, create `.planning/phases/02-jira-auth-task-fetching/02-01-SUMMARY.md`
</output>
