---
phase: 02-jira-auth-task-fetching
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [src/app/dashboard/page.tsx, src/app/api/tasks/import/route.ts, src/models/Task.ts]
autonomous: true
requirements: [INT-01, INT-02, INT-03]
must_haves:
  truths:
    - "User can enter their Jira PAT on the dashboard"
    - "User can search for tasks using JQL from the dashboard UI"
    - "User can click on a Jira task to import/assign it to themselves"
  artifacts:
    - path: "src/models/Task.ts"
      provides: "Local database model for tracked Jira tasks"
    - path: "src/app/api/tasks/import/route.ts"
      provides: "Logic for saving Jira metadata to local DB and self-assigning"
---

<objective>
Implement the frontend UI for Jira interaction on the dashboard and the logic to persist selected Jira tasks into our local tracking system.

Purpose: Bridges the gap between Jira's external data and our internal tracking/timeline system.
Output: Integrated search/import UI and Task data model.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-jira-auth-task-fetching/02-CONTEXT.md
@.planning/phases/02-jira-auth-task-fetching/02-01-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create local Task Model</name>
  <files>src/models/Task.ts</files>
  <read_first>src/models/User.ts</read_first>
  <action>Create `src/models/Task.ts` defining a Mongoose schema for local tasks. It should include: `jiraId` (string, unique), `key` (string), `summary` (string), `assignedTo` (ObjectId ref to User), `originalData` (Mixed/Object to store full Jira JSON), and placeholders for phase dates (e.g., `beAnalystStart`, `beAnalystDone`, etc., which will be used in Phase 3).</action>
  <verify>test -f src/models/Task.ts</verify>
  <acceptance_criteria>
    - src/models/Task.ts defines TaskSchema with jiraId and assignedTo
  </acceptance_criteria>
  <done>Task model created</done>
</task>

<task type="auto">
  <name>Task 2: Implement Import API</name>
  <files>src/app/api/tasks/import/route.ts</files>
  <read_first>src/models/Task.ts</read_first>
  <action>Create `src/app/api/tasks/import/route.ts`. This route should accept a POST request with the Jira issue object and the current user's info. It should save/upsert the task into MongoDB, setting `assignedTo` to the current user's ID.</action>
  <verify>grep "findOneAndUpdate" src/app/api/tasks/import/route.ts</verify>
  <acceptance_criteria>
    - API route exists for importing/saving tasks
    - Logic handles upserting based on jiraId
  </acceptance_criteria>
  <done>Import logic implemented</done>
</task>

<task type="auto">
  <name>Task 3: Build Jira Interface on Dashboard</name>
  <files>src/app/dashboard/page.tsx</files>
  <read_first>src/app/dashboard/page.tsx</read_first>
  <action>Update `src/app/dashboard/page.tsx`. Add a section (visible only if user has a role) to input a Jira Token (stored in local component state/session but not persisted). Provide a JQL search input field and a "Search" button. Display search results in a table or list. Add an "Import & Assign" button to each search result that calls the import API.</action>
  <verify>grep "Search" src/app/dashboard/page.tsx</verify>
  <acceptance_criteria>
    - Dashboard includes Jira Token and JQL inputs
    - Results display an "Import & Assign" action
  </acceptance_criteria>
  <done>Dashboard UI updated for Jira integration</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] User can walk through search -> results -> import flow.
- [ ] Imported tasks appear in the local MongoDB collection.
</verification>

<success_criteria>
- All tasks completed
- Jira integration is fully functional for task discovery and assignment
</success_criteria>

<output>
After completion, create `.planning/phases/02-jira-auth-task-fetching/02-02-SUMMARY.md`
</output>
