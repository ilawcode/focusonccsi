---
phase: 01-foundation-authentication
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/lib/db.ts, src/models/User.ts, src/app/api/auth/[...nextauth]/route.ts, src/app/(auth)/login/page.tsx, src/app/(auth)/register/page.tsx, src/app/dashboard/page.tsx, src/app/api/user/role/route.ts]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]
must_haves:
  truths:
    - "User can create a new account via the register page"
    - "User can log in with valid credentials"
    - "User is redirected to dashboard after login"
    - "User can select and save their role on the dashboard"
  artifacts:
    - path: "src/models/User.ts"
      provides: "Mongoose User model with role field"
    - path: "src/app/api/auth/[...nextauth]/route.ts"
      provides: "NextAuth session management"
  key_links:
    - from: "src/app/(auth)/register/page.tsx"
      to: "src/app/api/user/register/route.ts"
      via: "POST request to create user"
---

<objective>
Implement the Authentication system (NextAuth.js), MongoDB connectivity, and the Role Selection flow.

Purpose: Enables secure access and allows users to identify themselves within the role hierarchy required for the project tracking.
Output: Working login/registration system and a dashboard where users can set their roles.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-authentication/01-CONTEXT.md
@.planning/phases/01-foundation-authentication/01-01-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Database Setup and User Model</name>
  <files>src/lib/db.ts, src/models/User.ts</files>
  <read_first>.planning/phases/01-foundation-authentication/01-CONTEXT.md</read_first>
  <action>Create `src/lib/db.ts` to manage the Mongoose connection to MongoDB using a singleton pattern. Create `src/models/User.ts` defining a schema with fields: `name`, `surname`, `email` (unique), `password` (hashed), and `role` (enum: ['BE Analist', 'BE Dev', 'Web', 'Mobile'], default: null). Ensure `mongoose` is installed.</action>
  <verify>test -f src/models/User.ts && grep "role" src/models/User.ts</verify>
  <acceptance_criteria>
    - src/lib/db.ts exports a connection function/variable
    - src/models/User.ts defines UserSchema with email and role fields
  </acceptance_criteria>
  <done>Mongoose setup and User model created</done>
</task>

<task type="auto">
  <name>Task 2: NextAuth.js Integration</name>
  <files>src/app/api/auth/[...nextauth]/route.ts, .env.local</files>
  <read_first>src/models/User.ts</read_first>
  <action>Install `next-auth` and `bcryptjs`. Configure NextAuth using the Credentials provider. In the `authorize` function, use Mongoose to find the user by email and verify the password with `bcryptjs`. Add the `role` and `name` to the JWT and session objects via callbacks. Generate a `NEXTAUTH_SECRET` for `.env.local`.</action>
  <verify>grep "NextAuth" src/app/api/auth/[...nextauth]/route.ts</verify>
  <acceptance_criteria>
    - NextAuth callbacks include role in session
    - bcryptjs is used for password verification
  </acceptance_criteria>
  <done>Authentication logic implemented</done>
</task>

<task type="auto">
  <name>Task 3: Auth Pages (Login & Register)</name>
  <files>src/app/(auth)/login/page.tsx, src/app/(auth)/register/page.tsx</files>
  <read_first>src/app/globals.css</read_first>
  <action>Create visually stunning login and registration pages using Bootstrap classes and custom glassmorphism styles defined in Phase 01. The registration page should capture Name, Surname, Email, and Password. Use `signIn("credentials", ...)` from NextAuth on the login page. Ensure input validation is present.</action>
  <verify>test -f src/app/(auth)/login/page.tsx && test -f src/app/(auth)/register/page.tsx</verify>
  <acceptance_criteria>
    - Registration form includes Name, Surname, Email, Password
    - Login form calls next-auth signIn
    - Both pages follow the Dark Theme aesthetic
  </acceptance_criteria>
  <done>Auth UI pages created</done>
</task>

<task type="auto">
  <name>Task 4: Dashboard & Role Selection</name>
  <files>src/app/dashboard/page.tsx, src/app/api/user/role/route.ts</files>
  <read_first>src/app/api/auth/[...nextauth]/route.ts</read_first>
  <action>Create `src/app/dashboard/page.tsx`. If the user is logged in but has no role assigned, show a prominent "Select Your Role" modal/panel with options: BE Analist, BE Dev, Web, Mobile. Create an API route `src/app/api/user/role/route.ts` to update the user's role in MongoDB. Once saved, refresh the session or page to reflect the new role.</action>
  <verify>grep "Select Your Role" src/app/dashboard/page.tsx</verify>
  <acceptance_criteria>
    - Dashboard checks if role is null and prompts user
    - API route exists to update user role in DB
  </acceptance_criteria>
  <done>Role selection flow implemented</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] User can walk through full flow: Register -> Login -> Select Role.
- [ ] Session persists role information.
</verification>

<success_criteria>
- All tasks completed
- Full Auth + Role flow functional
- UI consistent with Dark/Premium theme
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-authentication/01-02-SUMMARY.md`
</output>
