---
phase: 01-foundation-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, tsconfig.json, src/app/layout.tsx, src/app/globals.css, src/app/page.tsx]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]
must_haves:
  truths:
    - "Next.js boots and serves pages without errors"
    - "Bootstrap CSS is included globally"
    - "Dark theme is applied as the default style"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Custom Vanilla CSS and dark mode tokens"
    - path: "package.json"
      provides: "Next.js project foundation"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "Stylesheet import"
---

<objective>
Initialize the Next.js project and set up the foundational UI, specifically Bootstrap and the custom dynamic Dark Theme.

Purpose: Creates the architectural foundation on which logic and auth will be built.
Output: Bootstrapped Next.js repository with Bootstrap CSS, custom dark mode styling.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-authentication/01-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Bootstrap Next.js App</name>
  <files>package.json, tsconfig.json, next.config.mjs</files>
  <read_first>.planning/phases/01-foundation-authentication/01-CONTEXT.md</read_first>
  <action>Run the create-next-app initialization. The command should be run in a way that generates the app directly in the current directory (`.`), ensuring TypeScript and App Router are enabled, but Tailwind is FALSE (`npx -y create-next-app@latest . --typescript --eslint --tailwind=false --app --src-dir --import-alias="@/*" --use-npm`). Then install bootstrap: `npm install bootstrap`.</action>
  <verify>test -f package.json && grep "next" package.json</verify>
  <acceptance_criteria>
    - package.json contains 'next' in dependencies
    - src/app directory exists
    - package.json contains 'bootstrap' in dependencies
  </acceptance_criteria>
  <done>Next.js is successfully initialized</done>
</task>

<task type="auto">
  <name>Task 2: Configure Global Styles and Bootstrap</name>
  <files>src/app/layout.tsx, src/app/globals.css</files>
  <read_first>src/app/layout.tsx, src/app/globals.css</read_first>
  <action>Modify `src/app/layout.tsx` to import `bootstrap/dist/css/bootstrap.min.css`. In `src/app/globals.css`, write custom CSS tokens to implement a vibrant, premium dark theme. Set root body background to a dark color (e.g. `#121212` or `#0b0f19`) and font color to white/off-white (`#e2e8f0`). Add basic transition utility classes for dynamic styling (`.glass-panel`, `.hover-glow`). Do NOT use Tailwind classes.</action>
  <verify>grep "bootstrap.min.css" src/app/layout.tsx</verify>
  <acceptance_criteria>
    - src/app/layout.tsx contains 'import "bootstrap/dist/css/bootstrap.min.css"'
    - src/app/globals.css contains body styling indicating dark mode
  </acceptance_criteria>
  <done>Styles configured and dark mode tokens added</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Home Page</name>
  <files>src/app/page.tsx</files>
  <read_first>src/app/page.tsx</read_first>
  <action>Clear the default Next.js boilerplate in `src/app/page.tsx`. Create a simple landing container acting as a placeholder using Bootstrap grid classes (`container`, `row`, `col-md-12`) thanking the user and containing a button leading to `/login`. Use custom CSS classes for a glassmorphism look (e.g. `.glass-panel`).</action>
  <verify>grep "container" src/app/page.tsx</verify>
  <acceptance_criteria>
    - src/app/page.tsx exports a default function
    - src/app/page.tsx uses Bootstrap container and row classes
  </acceptance_criteria>
  <done>Landing page is simplified and visually tested through code</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `package.json` contains required dependencies.
- [ ] Next.js app builds successfully (`npm run build`).
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-authentication/01-01-SUMMARY.md`
</output>
