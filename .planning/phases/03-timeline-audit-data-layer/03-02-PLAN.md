---
phase: 03-timeline-audit-data-layer
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [src/app/api/tasks/[id]/timeline/route.ts]
autonomous: true
requirements: [TASK-01, TASK-02, TASK-03]
must_haves:
  truths:
    - "Only users with the correct role can update their respective phase dates"
    - "Every valid date update creates an entry in the AuditLog collection"
    - "Dates are stored with precise timestamp (Date object)"
  artifacts:
    - path: "src/app/api/tasks/[id]/timeline/route.ts"
      provides: "Secure API for role-based timeline updates and auditing"
---

<objective>
Implement the core business logic for updating task timeline dates, including role-based permission enforcement and automatic audit logging.

Purpose: Ensures data integrity and provides a verifiable trail of all project tracking changes.
Output: Secure timeline update API route.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-timeline-audit-data-layer/03-CONTEXT.md
@.planning/phases/03-timeline-audit-data-layer/03-01-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement Timeline Update API</name>
  <files>src/app/api/tasks/[id]/timeline/route.ts</files>
  <read_first>src/models/Task.ts, src/models/AuditLog.ts</read_first>
  <action>Create `src/app/api/tasks/[id]/timeline/route.ts` (PATCH). The route should verify the user session and role. It should only allow updating dates that match the user's role (e.g., "BE Analist" can only update `beAnalystStart` or `beAnalystDone`). Before updating the `Task` document, it must create a new `AuditLog` entry for each changed field.</action>
  <verify>grep "AuditLog.create" src/app/api/tasks/[id]/timeline/route.ts</verify>
  <acceptance_criteria>
    - API enforces role-based permissions (one role = specific fields)
    - AuditLog entries are created for every change
    - Both Start and Done dates are supported for the role
  </acceptance_criteria>
  <done>Timeline API with auditing implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement Audit History Fetching</name>
  <files>src/app/api/tasks/[id]/history/route.ts</files>
  <read_first>src/models/AuditLog.ts</read_first>
  <action>Create `src/app/api/tasks/[id]/history/route.ts` (GET). This route should fetch all `AuditLog` entries for a given `taskId`, including populated user names, and return them sorted by date (newest first).</action>
  <verify>grep "populate" src/app/api/tasks/[id]/history/route.ts</verify>
  <acceptance_criteria>
    - API returns a history of changes for a task
    - Results include who made the change
  </acceptance_criteria>
  <done>History API implemented</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] Updating a date as the correct role succeeds and creates an audit log.
- [ ] Attempting to update a date as an incorrect role returns a 403 Forbidden error.
</verification>

<success_criteria>
- All tasks completed
- Backend logic for role-based tracking and auditing is fully functional
</success_criteria>

<output>
After completion, create `.planning/phases/03-timeline-audit-data-layer/03-02-SUMMARY.md`
</output>
