---
phase: 03-timeline-audit-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/models/User.ts, src/models/Task.ts, src/models/AuditLog.ts]
autonomous: true
requirements: [TASK-01, TASK-03]
must_haves:
  truths:
    - "User model role enum is expanded to include Analysis/Dev variants"
    - "Task model includes all start/done date fields for the expanded roles"
    - "AuditLog collection exists with taskId, userId, and change tracking"
  artifacts:
    - path: "src/models/AuditLog.ts"
      provides: "Schema for tracking historical changes to task dates"
---

<objective>
Update the core data models to support the expanded role list and the full suit of phase timeline dates, and establish the AuditLog schema.

Purpose: Provides the data infrastructure required for granular role-based tracking and full history transparency.
Output: Updated User/Task models and a new AuditLog model.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-timeline-audit-data-layer/03-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Update User Role Enum</name>
  <files>src/models/User.ts</files>
  <read_first>src/models/User.ts</read_first>
  <action>Modify `src/models/User.ts` to update the `role` field enum. New list: ["BE Analist", "BE Dev", "Web Analist", "Web Dev", "Mobile Analist", "Mobile Dev", "Test"]. Ensure defaults remain `null`.</action>
  <verify>grep "Web Analist" src/models/User.ts</verify>
  <acceptance_criteria>
    - User model role enum includes all 7 specific roles
  </acceptance_criteria>
  <done>User roles updated</done>
</task>

<task type="auto">
  <name>Task 2: Expand Task Phase Dates</name>
  <files>src/models/Task.ts</files>
  <read_first>src/models/Task.ts</read_first>
  <action>Update `src/models/Task.ts` to include full start/done dates for: `beAnalyst`, `beDev`, `webAnalyst`, `webDev`, `mobileAnalyst`, `mobileDev`, and `test`. Each should be of type `Date` with default `null`.</action>
  <verify>grep "webAnalystStart" src/models/Task.ts</verify>
  <acceptance_criteria>
    - Task model includes start/done fields for all 7 phase-role pairings
  </acceptance_criteria>
  <done>Task phase fields expanded</done>
</task>

<task type="auto">
  <name>Task 3: Create AuditLog Model</name>
  <files>src/models/AuditLog.ts</files>
  <read_first>src/models/User.ts</read_first>
  <action>Create `src/models/AuditLog.ts`. Schema should include: `taskId` (ObjectId ref Task), `userId` (ObjectId ref User), `field` (string - the date field changed), `oldValue` (Date), `newValue` (Date). Include `timestamps: true` to get the 'when' (createdAt).</action>
  <verify>test -f src/models/AuditLog.ts</verify>
  <acceptance_criteria>
    - AuditLog model created with taskId and userId references
  </acceptance_criteria>
  <done>AuditLog model created</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] Mongoose models can be imported without errors.
</verification>

<success_criteria>
- All tasks completed
- Data models reflect the updated project tracking domain
</success_criteria>

<output>
After completion, create `.planning/phases/03-timeline-audit-data-layer/03-01-SUMMARY.md`
</output>
