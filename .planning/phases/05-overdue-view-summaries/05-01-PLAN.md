---
phase: 05-overdue-view-summaries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/RoleAnalyticsWidget.tsx, src/components/TaskTable.tsx, src/app/dashboard/page.tsx]
autonomous: true
requirements: [DASH-04, DASH-05]
must_haves:
  truths:
    - "Analytics calculations are tied strictly to the logged-in user's role"
    - "Overdue definition uses Current Date > Task[role_Done_Date]"
    - "Overdue dates in the TaskTable highlight vividly (e.g. text-danger)"
  artifacts:
    - path: "src/components/RoleAnalyticsWidget.tsx"
      provides: "Component rendering cards for Total, This Week, and Overdue"
---

<objective>
Develop the role-centric analytics widget and update the timeline visualization to explicitly flag overdue phases.

Purpose: Empowers users to immediately recognize missed or impending deadlines within their scope of responsibility without manual date comparison.
Output: Active analytics cards on the dashboard and conditional formatting in the Date table.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-overdue-view-summaries/05-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create RoleAnalyticsWidget Component</name>
  <files>src/components/RoleAnalyticsWidget.tsx</files>
  <read_first>src/app/dashboard/page.tsx</read_first>
  <action>Create `src/components/RoleAnalyticsWidget.tsx`. It accepts `tasks` (array) and `userRole` (string). Determine the 'Done' field key based on `userRole` (e.g., "BE Dev" -> `beDevDone`). Compute 3 metrics: 1) Total tracked tasks (where the Done date is set), 2) Overdue (Done date exists and is in the past), 3) Due This Week (Done date is in the future but within the next 7 days). Display these 3 metrics using aesthetically premium summary cards (vibrant colors, glassmorphism).</action>
  <verify>grep "Overdue" src/components/RoleAnalyticsWidget.tsx</verify>
  <acceptance_criteria>
    - Calculations isolate to the specific Role's date field
    - Overdue uses native Date comparisons against Date.now()
    - Component exposes 3 distinct metrics
  </acceptance_criteria>
  <done>RoleAnalyticsWidget created</done>
</task>

<task type="auto">
  <name>Task 2: Inject Analytics into Dashboard</name>
  <files>src/app/dashboard/page.tsx</files>
  <read_first>src/components/RoleAnalyticsWidget.tsx</read_first>
  <action>Update `src/app/dashboard/page.tsx`. In the `activeTab === 'timeline'` conditionally rendered section, render the newly created `<RoleAnalyticsWidget tasks={tasks} userRole={userRole} />` just above the `TaskTable`. Remove the old Phase 5 placeholder ("Role Analytics").</action>
  <verify>grep "RoleAnalyticsWidget" src/app/dashboard/page.tsx</verify>
  <acceptance_criteria>
    - Widget displays properly above the timeline table
  </acceptance_criteria>
  <done>Analytics widget integrated</done>
</task>

<task type="auto">
  <name>Task 3: Highlight Overdue Dates in TaskTable</name>
  <files>src/components/TaskTable.tsx</files>
  <read_first>src/components/TaskTable.tsx</read_first>
  <action>Modify `src/components/TaskTable.tsx`. Within `PhaseCell`, add logic to determine if the injected date (`val`) is a 'Done' field AND is in the past (`new Date(val) < new Date()`). If true, change its rendering class from `text-light` (or whatever the default is) to a danger state (e.g., `text-danger fw-bold`). To know if it's a 'Done' field, pass an `isDone` boolean into `PhaseCell` or deduce it from the `label`.</action>
  <verify>grep "text-danger" src/components/TaskTable.tsx</verify>
  <acceptance_criteria>
    - Past 'Done' dates are visually distinct (e.g., red text)
    - Future dates or 'Start' dates are unaffected by the overdue text coloring
  </acceptance_criteria>
  <done>TaskTable conditional formatting implemented</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] Analytics widget renders metrics without errors when task list is empty.
- [ ] TaskTable correctly highlights an overdue "Done" date in development preview.
</verification>

<success_criteria>
- Project incorporates functional reporting tools specific to user roles.
- Delayed data is visually isolated.
</success_criteria>

<output>
After completion, create `.planning/phases/05-overdue-view-summaries/05-01-SUMMARY.md`
</output>
