---
phase: 04-main-dashboard-visualization
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [src/app/dashboard/page.tsx, src/components/TaskTable.tsx, src/components/GanttChart.tsx, src/components/EditDateModal.tsx, src/components/TaskHistory.tsx]
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03]
must_haves:
  truths:
    - "Dashboard shows a sortable task table view"
    - "Dashboard shows a Gantt chart with color-coded modified dates"
    - "Edit flow opens a modal with date picker and character-counted reason textarea"
    - "History panel is togglable per task showing dated entries with reasons"
  artifacts:
    - path: "src/components/GanttChart.tsx"
      provides: "Chromatic Gantt visualization of all task phases"
    - path: "src/components/EditDateModal.tsx"
      provides: "Modal with 100-char reason enforcement and date picker"
---

<objective>
Build the full dashboard visualization UI: a task table, chromatic Gantt chart, and the enforced edit modal with history viewer.

Purpose: Delivers the primary user interface for project tracking and date management.
Output: Feature-complete interactive dashboard with Gantt + Table dual views.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-main-dashboard-visualization/04-CONTEXT.md
@.planning/phases/04-main-dashboard-visualization/04-01-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create EditDateModal Component</name>
  <files>src/components/EditDateModal.tsx</files>
  <read_first>.planning/phases/04-main-dashboard-visualization/04-CONTEXT.md</read_first>
  <action>Create `src/components/EditDateModal.tsx`. A Bootstrap modal component that receives: `taskId`, `field`, `currentValue`, `isFirstEntry` (bool), and `onClose`/`onSave` callbacks. Render: a datetime-local input for the new date. If NOT first entry, show a textarea with live character counter (show "X / 100" chars) and disable the Save button until 100+ chars reached. On save, calls PATCH `/api/tasks/[taskId]/timeline`.</action>
  <verify>grep "reason.length" src/components/EditDateModal.tsx</verify>
  <acceptance_criteria>
    - Modal shows date picker
    - Character counter shown, Save disabled until >= 100 chars (for modifications)
    - Calls the timeline API on save
  </acceptance_criteria>
  <done>EditDateModal created</done>
</task>

<task type="auto">
  <name>Task 2: Create TaskHistory Component</name>
  <files>src/components/TaskHistory.tsx</files>
  <read_first>src/models/AuditLog.ts</read_first>
  <action>Create `src/components/TaskHistory.tsx`. Renders a collapsible section that fetches from `GET /api/tasks/[taskId]/history` when first expanded. Display each log entry as: who changed it, which field, from/to values, when, and the reason text. Most recent entry at top.</action>
  <verify>grep "history" src/components/TaskHistory.tsx</verify>
  <acceptance_criteria>
    - Shows chronological log entries with user name, field, dates, and reason
    - Lazy-loads: only fetches when expanded
  </acceptance_criteria>
  <done>TaskHistory component created</done>
</task>

<task type="auto">
  <name>Task 3: Create GanttChart Component</name>
  <files>src/components/GanttChart.tsx</files>
  <read_first>src/models/Task.ts</read_first>
  <action>Create `src/components/GanttChart.tsx`. A CSS/SVG based horizontal bar chart. Each row is a task. Each phase (BE Analyst, BE Dev, Web Analyst, Web Dev, Mobile Analyst, Mobile Dev, Test) appears as a colored horizontal bar based on Start/Done dates. Color coding: original entry = blue, modified entry = amber/orange. Show tooltip with latest reason on hover. Scale the chart to a reasonable date range. Use the dark theme CSS variables.</action>
  <verify>grep "webAnalystStart" src/components/GanttChart.tsx</verify>
  <acceptance_criteria>
    - Renders one row per task
    - Uses distinct colors for original vs modified dates
    - Hover shows the latest reason from AuditLog
  </acceptance_criteria>
  <done>GanttChart created</done>
</task>

<task type="auto">
  <name>Task 4: Assemble Main Dashboard Page</name>
  <files>src/app/dashboard/page.tsx</files>
  <read_first>src/app/dashboard/page.tsx, src/components/GanttChart.tsx, src/components/EditDateModal.tsx, src/components/TaskHistory.tsx</read_first>
  <action>Update `src/app/dashboard/page.tsx`. After role selection, fetch all tasks from `GET /api/tasks` (role-sorted). Show two tabs (or toggle): "Table View" and "Gantt View". Table: show each task with all phase date cells. Each date cell: displays date + small "edit" icon (only for the user's role's fields). Clicking edit opens EditDateModal. Each task row has an expandable "History" toggle that loads TaskHistory. Keep the existing Jira Import section but move it to a separate "Import" tab. Gantt: render the GanttChart component with the fetched tasks.</action>
  <verify>grep "GanttChart" src/app/dashboard/page.tsx</verify>
  <acceptance_criteria>
    - Dashboard tabs: Timeline, Gantt, Import
    - Table shows all tasks with all phase dates
    - User can only edit their own role's columns
    - History toggle works per row
    - Gantt chart rendered properly
  </acceptance_criteria>
  <done>Dashboard fully assembled</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] Timeline tab loads and shows sorted tasks.
- [ ] Clicking an edit button (for user's role) opens modal.
- [ ] Saving without 100-char reason is blocked.
- [ ] History panel shows log with reasons.
- [ ] Gantt tab renders bars on a timeline.
</verification>

<success_criteria>
- Full dashboard UX is functional and visually premium
</success_criteria>

<output>
After completion, create `.planning/phases/04-main-dashboard-visualization/04-02-SUMMARY.md`
</output>
