---
phase: 04-main-dashboard-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/models/AuditLog.ts, src/app/api/tasks/[id]/timeline/route.ts, src/app/api/tasks/route.ts]
autonomous: true
requirements: [DASH-01, DASH-02]
must_haves:
  truths:
    - "AuditLog model includes a `reason` field"
    - "Timeline PATCH API validates 100-char reason on change (not first entry)"
    - "GET /api/tasks returns all tasks sorted by the caller's role deadline"
  artifacts:
    - path: "src/app/api/tasks/route.ts"
      provides: "Fetches all tasks sorted by the logged-in user's relevant deadline"
---

<objective>
Update the backend to support the mandatory justification workflow and provide a sorted task-listing API for the dashboard.

Purpose: Ensures data integrity at the API level before building the UI.
Output: Updated AuditLog model, revised Timeline API with reason enforcement, and a new tasks listing endpoint.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-main-dashboard-visualization/04-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add `reason` field to AuditLog</name>
  <files>src/models/AuditLog.ts</files>
  <read_first>src/models/AuditLog.ts</read_first>
  <action>Add a `reason` field (type: String, required: false, default: null) to the AuditLog schema. This will store the justification text for each date change.</action>
  <verify>grep "reason" src/models/AuditLog.ts</verify>
  <acceptance_criteria>
    - AuditLog has a `reason` field
  </acceptance_criteria>
  <done>AuditLog updated</done>
</task>

<task type="auto">
  <name>Task 2: Enforce 100-char reason in Timeline API</name>
  <files>src/app/api/tasks/[id]/timeline/route.ts</files>
  <read_first>src/app/api/tasks/[id]/timeline/route.ts</read_first>
  <action>Update the PATCH handler in `src/app/api/tasks/[id]/timeline/route.ts`. Accept a `reason` field in the request body. Logic: if the existing field value is NOT null (i.e., it's a modification, not first entry), then `reason` must be provided and at least 100 characters long — otherwise return 400. Pass `reason` through to AuditLog entries.</action>
  <verify>grep "reason.length" src/app/api/tasks/[id]/timeline/route.ts</verify>
  <acceptance_criteria>
    - First-time entry (null -> value) does NOT require a reason
    - Any modification (value -> new value) requires reason >= 100 chars
    - AuditLog entries include the reason
  </acceptance_criteria>
  <done>Reason enforcement implemented</done>
</task>

<task type="auto">
  <name>Task 3: Create Tasks listing API with role-based sorting</name>
  <files>src/app/api/tasks/route.ts</files>
  <read_first>src/models/Task.ts, src/models/User.ts</read_first>
  <action>Create `src/app/api/tasks/route.ts` (GET). Get the current user session and their role. Map the role to the relevant date field (e.g., "BE Analist" -> "beAnalystDone"). Fetch all tasks from MongoDB, then sort by that date field ascending (null dates last). Return the sorted array. Include the populated `assignedTo` (name, role).</action>
  <verify>grep "sort" src/app/api/tasks/route.ts</verify>
  <acceptance_criteria>
    - Tasks are sorted by the logged-in user's role deadline
    - Null dates appear last in the sort
    - All tasks are returned (not just the user's own)
  </acceptance_criteria>
  <done>Tasks listing API implemented</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] Modification without a 100-char reason returns 400.
- [ ] First entry without reason succeeds.
</verification>

<success_criteria>
- Backend is fully prepared for the dashboard UI
</success_criteria>

<output>
After completion, create `.planning/phases/04-main-dashboard-visualization/04-01-SUMMARY.md`
</output>
